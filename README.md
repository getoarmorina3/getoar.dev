# getoar.dev

Personal portfolio — Next.js App Router, MDX writing, CSS Modules.

```bash
pnpm install
pnpm dev
```

Optional: set `NEXT_PUBLIC_SITE_URL` for canonical/OG URLs (defaults to `https://getoar.dev`).
